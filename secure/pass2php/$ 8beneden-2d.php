<?php
$level=0;
$item='Rbureel';
if ($d[$item]['s']>$level) {
	sl($item, $level, basename(__FILE__).':'.__LINE__);
}
$item='RkeukenL';
if ($d[$item]['s']>$level) {
	sl($item, $level, basename(__FILE__).':'.__LINE__);
}
$item='RkeukenR';
if ($d[$item]['s']>$level) {
	sl($item, $level, basename(__FILE__).':'.__LINE__);
}
